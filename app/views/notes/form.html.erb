<%= form_for @note do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br />
  <%= f.label :body %>
  <%= f.text_box :body %>
  <%= f.submit %>
  <% end %>

  <p>
  <%= link_to 'Home Page', root_path %>